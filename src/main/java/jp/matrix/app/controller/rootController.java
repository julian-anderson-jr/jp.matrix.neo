package jp.matrix.app.controller;
/*
import org.springframework.web.bind.annotation.RequestMethod;

//import jp.matrix.app.AppConfig;
//import jp.matrix.dao.JdbcUserRepository;
import jp.matrix.service.SessionCartItem;

//import javax.activation.DataSource;

import org.springframework.beans.factory.annotation.Autowired;
//import org.springframework.context.ApplicationContext;
//import org.springframework.context.annotation.AnnotationConfigApplicationContext;
import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.web.bind.annotation.RequestMapping;

@Controller
@RequestMapping("/root")
public class rootController {
	@Autowired
	private SessionCartItem session;
	
	@RequestMapping(method = RequestMethod.GET)
	public String index(Model mv)
	{
		//ApplicationContext context = (ApplicationContext)new AnnotationConfigApplicationContext(AppConfig.class);
		//new JdbcUserRepository((javax.sql.DataSource)context.getBean("dataSource", DataSource.class));
		return "/index.html";
	}
	@RequestMapping(method = RequestMethod.POST)
	public String change(Model mv)
	{
		return "/index.html";
	}
}
*/